<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.Pothole" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here -->
        <item name="colorPrimary">@color/my_light_primary</item>
        <item name="colorPrimaryVariant">@color/my_light_primary_variant</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Status Bar Color -->
        <item name="android:statusBarColor">@color/my_light_primary</item>
        <!-- Set status bar icons and text to dark or light -->
        <item name="android:windowLightStatusBar">true</item> <!-- Text/icon màu đen -->
    </style>

    <!-- Default theme for the app -->
    <style name="Theme.Pothole" parent="Base.Theme.Pothole" />
</resources>
